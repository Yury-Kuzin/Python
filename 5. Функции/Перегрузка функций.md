## Перегрузка функций

Возможность создать несколько функций с одним именем, но разными параметрами (разное кол-во или разный тип).\
В python нет стандартных способов перегрузки функций.

### Способ I



### Способ II :trophy:

Использование библиотеки <code>multipledispatch</code>.

```python
from multipledispatch import dispatch


@dispatch(int, int, int)
def check_date(day: int, month: int, year: int):
    return TODO


@dispatch(str)
def check_date(date: str):
    return TODO


print(check_date(5, 7, 85))
```
